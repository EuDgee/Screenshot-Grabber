package ru.livetex.grabber

import org.scalatest.FlatSpec

class RequestParseActor extends FlatSpec {
  "Request Parse Actor" should "extract requested url" in {pending}

  it should "check URL correctness" in {pending}

  it should "check URL accessibility" in {pending}
}
