package ru.livetex.grabber

import org.scalatest.FlatSpec

class HttpListenerActorSpec extends FlatSpec {
  "Http Listener Actor" should "listen to requests an specified address and port" in {pending}

  it should "pipe messages to a request parse actor" in {pending}
}
